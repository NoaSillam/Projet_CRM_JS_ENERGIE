<form method="post" action="{{ path('app_bath_delete', {'id': bath.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bath.id) }}">
    <button class="btn">Delete</button>
</form>
